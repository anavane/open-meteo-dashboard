# Open-Meteo Interactive Weather Dashboard (Streamlit)

Click on the map to fetch hourly and daily weather data from the Open-Meteo API.

## Run locally

1. Create and activate a virtual environment:
   ```bash
   python -m venv venv
   source venv/bin/activate   # mac/linux
   venv\Scripts\activate      # windows
